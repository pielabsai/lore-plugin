---
description: Walk the user through connecting the Lore plugin — credentials, namespace schema, and optional GitHub PR seeding.
---

The user invoked the `/lore-setup` slash command. Run the `lore-setup` skill from this plugin to walk them through the full setup flow:

1. Credentials (App ID, Namespace ID, API key)
2. Namespace schema (ask what kinds of things they'd like remembered, then configure the namespace addendum)
3. Optional GitHub PR seeding (if this workspace is a GitHub repo)

Follow the skill's instructions exactly. Do not skip steps.
